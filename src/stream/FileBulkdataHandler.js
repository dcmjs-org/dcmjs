/**
 * Handles bulkdata that gets written out to a file as hash encoded bulkdata.
 * By default, this will NOT read the entire block into memory, but will stream it
 * out, and will provide a BulkData uri value in it's place.
 */
export class FileBulkdataHandler {}
